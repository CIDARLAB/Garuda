
Fix commented input in *{...}.html
    import
    search
    recommender

Getting a 400 on wrong login
    Try returning ResponseEntity instead of body

    Or try overriding exception handler and create ResponseEntityExceptionHandler class
    
    @Override
    protected ResponseEntity<Object> handleMissingServletRequestParameter(MissingServletRequestParameterException ex, HttpHeaders headers, HttpStatus status, WebRequest request) {
        log.warn("miss Request Param");

        return new ResponseEntity<>(new FoxResponse(ErrorCode.ARG_INVALID), status);
    }

DELETE not supported in HTML5??

https://softwareengineering.stackexchange.com/questions/114156/why-are-there-are-no-put-and-delete-methods-on-html-forms


Workarounds:

Post tunneling,
Session destroyed listener 

